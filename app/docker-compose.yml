version: '3.8'
services:
  app:
    build:
      context: .
      args: 
        RUNTIME_ID: linux-x64
    environment:
      NOTION_DATABASE_ID:
      NOTION_TOKEN:
      CI:
    ports:
      - 5000:5000
